version: 2.0
jobs:
  build:
    docker:
      - image: python:2.7
    working_directory: /gordon-test
    steps:
      - run: cat adsfasdfasdfadsf

  deploy:
    docker:
      - image: python:2.7
    working_directory: /gordon-test
    steps:
      - run:
          command: echo "deploying! (not really) "
          pwd: /


  longtime:
    docker:
      - image: python:2.7
    working_directory: /gordon-test
    steps:
      - run:
          command: echo "deploying! (not really) "
          pwd: /

      - run: sleep 60

workflows:
  version: 2
  build-n-deploy:
    zobs:
      # - build
      - longtime
      # - deploy:
      #     requires:
      #       - build

# # testing bad config
# workflows:
#   version: 2
#   build-and-test:
#     jobs:
#       - build
#       - deploy-dev:
#         requires:
#           - build
